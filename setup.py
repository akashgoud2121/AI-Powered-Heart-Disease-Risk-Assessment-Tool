from setuptools import setup, find_packages

setup(
    name="heart-disease-prediction",
    version="1.0.0",
    packages=find_packages(),
    install_requires=[
        "setuptools>=68.0.0",
        "wheel>=0.40.0",
    ],
)